# JPA Entity ↔ DDL 매핑 검증 리포트

## 📋 검증 대상

| JPA Entity | DDL 테이블 | 스키마 |
|------------|-----------|--------|
| `AppUser.kt` | `APP_USER` | `SPEECHAPP_USER` |
| `UserProfile.kt` | `USER_PROFILE` | `SPEECHAPP_USER` |

---

## 1. AppUser.kt ↔ APP_USER

### 1.1 필드 매핑

| JPA 필드 | JPA 애너테이션 | DDL 컬럼 | DDL 타입 | DDL 제약 | 일치 |
|----------|---------------|----------|----------|----------|------|
| `id: Long?` | `@Id`, `@GeneratedValue(IDENTITY)` | `id` | `NUMBER(19)` | PK, `GENERATED BY DEFAULT AS IDENTITY` | ✅ |
| `uuid: String` | `nullable=false, unique=true` | `uuid` | `VARCHAR2(36)` | `NOT NULL`, `UNIQUE` | ✅ |
| `firebaseUid: String` | `nullable=false, unique=true, length=128` | `firebase_uid` | `VARCHAR2(255)` | `UNIQUE` | ⚠️ |
| `email: String` | `nullable=false, unique=true, length=255` | `email` | `VARCHAR2(255)` | `UNIQUE` | ⚠️ |
| `createdAt: LocalDateTime?` | `@CreationTimestamp`, `nullable=false` | `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | ✅ |
| `updatedAt: LocalDateTime?` | `@UpdateTimestamp`, `nullable=false` | `updated_at` | `TIMESTAMP` | — | ✅ |

### 1.2 매핑 불일치 항목 상세

| 항목 | JPA | DDL | 조치 |
|------|-----|-----|------|
| `firebase_uid` nullable | `nullable=false` (NOT NULL) | `UNIQUE` (nullable) | **불일치** — JPA는 NOT NULL, DDL은 nullable |
| `email` nullable | `nullable=false` (NOT NULL) | `UNIQUE` (nullable) | **불일치** — JPA는 NOT NULL, DDL은 nullable |
| `firebase_uid` 길이 | `length=128` | `VARCHAR2(255)` | 오케이 — DDL이 더 큼 |

### 1.3 확장 컬럼 (JPA에 없음, DDL에만 존재)

| DDL 컬럼 | 타입 | nullable | 용도 |
|----------|------|----------|------|
| `social_provider` | `VARCHAR2(20)` | ✅ | Naver/Kakao 확장 |
| `social_id` | `VARCHAR2(255)` | ✅ | 소셜 플랫폼 고유 ID |

> ℹ️ 확장 컬럼은 `nullable`이므로 기존 JPA와 호환됨. JPA에서 `social_provider`, `social_id` 추가 시 `@Column(nullable=true)`로 매핑.

### 1.4 제약조건 매핑

| JPA | DDL | 상태 |
|-----|-----|------|
| `@Table(name="app_user")` | 테이블명 `app_user` (Oracle 내부 `APP_USER`) | ✅ |
| `@Id` | `PRIMARY KEY` | ✅ |
| `@GeneratedValue(IDENTITY)` | `GENERATED BY DEFAULT AS IDENTITY` | ✅ |
| `@CreationTimestamp` | `DEFAULT CURRENT_TIMESTAMP` | ✅ |
| `@UpdateTimestamp` | — (JPA가 관리, Oracle `ON UPDATE` 미지원) | ✅ |

---

## 2. UserProfile.kt ↔ USER_PROFILE

### 2.1 필드 매핑

| JPA 필드 | JPA 애너테이션 | DDL 컬럼 | DDL 타입 | DDL 제약 | 일치 |
|----------|---------------|----------|----------|----------|------|
| `id: Long?` | `@Id`, `@GeneratedValue(IDENTITY)` | `id` | `NUMBER(19)` | PK, IDENTITY | ✅ |
| `user: AppUser?` | `@OneToOne`, `@JoinColumn(nullable=false, unique=true)` | `user_id` | `NUMBER(19)` | `NOT NULL`, `UNIQUE`, FK | ✅ |
| `nickname: String?` | `length=50` | `nickname` | `VARCHAR2(50)` | nullable | ✅ |
| `profileImageUrl: String?` | `length=500` | `profile_image_url` | `VARCHAR2(500)` | nullable | ✅ |
| `createdAt: LocalDateTime?` | `@CreationTimestamp`, `nullable=false` | `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | ✅ |
| `updatedAt: LocalDateTime?` | `@UpdateTimestamp`, `nullable=false` | `updated_at` | `TIMESTAMP` | — | ✅ |

### 2.2 제약조건 매핑

| JPA | DDL | 상태 |
|-----|-----|------|
| `@Table(name="user_profile")` | 테이블명 `user_profile` | ✅ |
| `@OneToOne` + `@JoinColumn` | `user_id` FK → `app_user(id)` + `UNIQUE` | ✅ |
| `@Id` + IDENTITY | PK, IDENTITY | ✅ |
| `@CreationTimestamp` / `@UpdateTimestamp` | `TIMESTAMP` | ✅ |

---

## 3. 전체 검증 요약

| 항목 | 상태 | 설명 |
|------|------|------|
| 테이블명 매핑 | ✅ | JPA `@Table(name=...)` ↔ DDL 테이블명 일치 |
| PK/IDENTITY | ✅ | `GenerationType.IDENTITY` ↔ Oracle IDENTITY |
| Timestamp | ✅ | `@CreationTimestamp`/`@UpdateTimestamp` ↔ `TIMESTAMP` |
| FK 제약 | ✅ | `@OneToOne` + `@JoinColumn` ↔ FK + UNIQUE |
| **firebase_uid nullable** | ⚠️ | JPA는 `NOT NULL`, DDL은 nullable |
| **email nullable** | ⚠️ | JPA는 `NOT NULL`, DDL은 nullable |
| VARCHAR2 길이 | ✅ | DDL 길이 ≥ JPA length |

---

## 4. 권장 조치

### 🔴 높은 우선순위: nullable 불일치

JPA `AppUser.kt`에서 `firebase_uid`와 `email`이 `nullable=false`인데, DDL에서는 nullable로 선언되어 있어.

**옵션 A:** JPA Entity 수정 (권장)
```kotlin
@Column(name = "firebase_uid", nullable = true, unique = true, length = 255)
val firebaseUid: String? = null,

@Column(name = "email", nullable = true, unique = true, length = 255)
val email: String? = null,
```

**옵션 B:** DDL 수정
```sql
firebase_uid VARCHAR2(255) NOT NULL UNIQUE,
email        VARCHAR2(255) NOT NULL UNIQUE,
```
> ⚠️ 소셜 로그인 확장 시 일부 플랫폼은 이메일 미제공 → `NOT NULL` 불가. **옵션 A 권장**.

---

## 5. 백엔드 세션 이관 사항 (TODO)

> ⚠️ **Database 세션에서 확인한 사항.** 백엔드 세션에서 아래 작업 필요.

| ID | 작업 | 파일 | 설명 |
|----|------|------|------|
| **BE-DB-01** | `AppUser.kt` nullable 수정 | `entity/AppUser.kt` | `firebase_uid`, `email`을 `nullable = false` → `nullable = true`로 변경. 소셜 로그인 확장 시 이메일 미제공 플랫폼 대응. |
| **BE-DB-02** | 소셜 확장 필드 추가 | `entity/AppUser.kt` | `social_provider` (VARCHAR2 20), `social_id` (VARCHAR2 255) 필드 추가. 현재 DDL에는 이미 포함됨. |
| **BE-DB-03** | `@Table` 스키마 명시 | `entity/AppUser.kt`, `entity/UserProfile.kt` | Oracle 스키마 분리 대응: `@Table(name = "app_user", schema = "speechapp_user")` 추가 |
| **BE-DB-04** | `application.yml` dev 프로필 수정 | `src/main/resources/application.yml` | `spring.datasource.url`을 H2 → Oracle XE로 변경. `speechapp_app` 계정 사용. |

---

검증 완료: 2026-08-27
