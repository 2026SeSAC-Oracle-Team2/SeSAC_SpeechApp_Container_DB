# ============================================================
# .env.example
# 실제 사용 시 .env로 복사 후 비밀번호 변경
-- ============================================================

# Oracle SYS/SYSTEM 비밀번호 (gvenzl 이미지에서 SYSTEM/SYS 계정 설정용)
# 주의: 이 값은 init SQL의 사용자 계정과 무관함.
# speechapp_app / speechapp_admin 비밀번호는 init/01_create_schemas.sql에서 직접 설정.
ORACLE_PASSWORD=ChangeMe123
